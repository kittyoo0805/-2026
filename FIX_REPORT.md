# 🛡️ 安全漏洞修复报告

> **项目**: Flask 用户管理平台
> **路径**: `/opt/Class01/`
> **GitHub**: https://github.com/kittyoo0805/-2026

---

## 一、SQL 注入漏洞（2 处）

| 位置 | 漏洞点 | 修复前 | 修复后 |
|------|--------|--------|--------|
| **搜索功能** | `keyword` 拼接到 SQL | `f"LIKE '%{keyword}%'"` | `LIKE ?` + 参数化查询 |
| **注册功能** | 表单字段拼接到 SQL | `f"VALUES ('{username}',...)"` | `VALUES (?, ?, ?, ?)` + 参数化查询 |

**原理**：用户输入中的 `'` 会闭合 SQL 字符串，导致 `OR '1'='1`、`UNION SELECT` 等注入攻击。

---

## 二、文件上传漏洞（3 个）

| 漏洞 | 位置 | 修复前 | 修复后 |
|:----|------|--------|--------|
| **任意文件上传** | `/upload` | 允许上传 `.html/.exe` 任意文件 | 白名单只允许图片格式 |
| **路径穿越** | `/upload` | `filename = file.filename` | `os.path.basename()` 过滤 `../` |
| **文件覆盖** | `/upload` | 同名文件直接覆盖 | 存在时自动重命名 `_1`、`_2` |

---

## 三、认证与授权漏洞（3 个）

| 漏洞 | 位置 | 修复前 | 修复后 |
|:----|------|--------|--------|
| **Session 密钥固定** | `app.config` | `"dev-key-2025"` 可被破解 | `secrets.token_hex(32)` 随机生成 |
| **Session 永不过期** | 无配置 | 登录一次永不过期 | 30 分钟自动过期 |
| **Debug 模式** | `app.run()` | `debug=True` 远程可执行代码 | `debug=False` |

---

## 四、密码安全漏洞（2 个）

| 漏洞 | 位置 | 修复前 | 修复后 |
|:----|------|--------|--------|
| **注册密码明文存储** | 注册路由 | 数据库存明文 `admin123` | `generate_password_hash()` 哈希存储 |
| **登录明文比对** | 登录路由 | `row[1] == password` | `check_password_hash()` 哈希比对 |

---

## 五、个人中心漏洞（3 个）

| 漏洞 | 位置 | 修复前 | 修复后 |
|:----|------|--------|--------|
| **水平越权**（查看资料） | `/profile` | `jack?user_id=1` 可看 admin | 只能看自己的资料 |
| **水平越权**（越权充值） | `/recharge` | `jack` 可给 admin 充值 | 只能给自己充值 |
| **负数金额**（业务逻辑） | `/recharge` | `amount=-99999` 扣钱成功 | `amount <= 0` 时拦截 |

---

## 六、安全响应头缺失

| 响应头 | 作用 |
|--------|------|
| `X-Frame-Options: DENY` | 防止点击劫持 |
| `X-Content-Type-Options: nosniff` | 防止 MIME 嗅探 |
| `Content-Security-Policy` | 防止 XSS |
| `Server: WebServer` | 隐藏版本号 |

---

## 七、Bug 修复

| Bug | 修复前 | 修复后 |
|:----|--------|--------|
| 登录后刷新重复提交 | URL 卡在 `/login` | `redirect("/")` 跳转 |
| 导航栏代码重复 | 4 个文件各有重复 | 统一继承 `base.html` |
| 点"个人中心"报错 | 必须手动提供 ID | 自动查询当前用户 ID |

---

## 修复验证结果

| 测试项 | 修复前 | 修复后 |
|--------|:------:|:------:|
| `' OR '1'='1` 搜索爆全部用户 | ✅ 成功 | ❌ 无搜索结果 |
| 上传 `.html` 恶意文件 | ✅ 成功 | ❌ 不支持的文件类型 |
| `../../etc` 路径穿越 | ✅ 穿越成功 | ❌ 只保留文件名 |
| `jack` 看 `admin` 资料 | ✅ 可以看到 | ❌ 无权查看 |
| `amount=-99999` 充负数 | ✅ 扣钱成功 | ❌ 金额必须大于 0 |
| Session 伪造 | ✅ 可破解 | ❌ 随机密钥不可预测 |
| 数据库密码 | ✅ 明文 | ❌ scrypt 哈希 |
| 个人中心无需手动填 ID | ❌ 报错 | ✅ 自动查询 |
